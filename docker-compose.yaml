services:
  # aisu-frontend:
  #   image: node:24-bookworm-slim
  #   container_name: aisu-frontend
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   env_file:
  #     - .env
  #   ports:
  #     - "3030:3000"
  # aisu-backend:
  #   image: node:24-bookworm-slim
  #   container_name: aisu-backend
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   env_file:
  #     - .env
  #   ports:
  #     - "3040:3040"
  aisu-postgres:
    image: postgres:16-alpine
    container_name: aisu-db
    restart: unless-stopped
    env_file:
      - ./backend/.env
    ports:
      - "3050:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
